export default class Car {
    constructor(id, mark, model, typ, safety) {
        this.id = id
        this.mark = mark
        this.model = model
        this.typ = typ
        this.safety = safety
    }
}